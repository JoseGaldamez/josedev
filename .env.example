OPENAI_API_KEY=api-key-openai
EMAIL_HOST=string
EMAIL_PORT=number
EMAIL_USER=email@email.com
EMAIL_PASSWORD=password
EMAIL_FROM='"Name" <email@email.com>'
EMAIL_TO=email@email.com
EMAIL_SUBJECT="String subject."

# Firebase Configuration
# Obtén estos valores desde la consola de Firebase (Project Settings > General > Your apps)
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyC0TP05lN_KL7ZGWM9pWwzjelZRtleM4Gc
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=josedev-website.firebaseapp.com
NEXT_PUBLIC_FIREBASE_DATABASE_URL=https://josedev-website-default-rtdb.firebaseio.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=josedev-website
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=josedev-website.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=777430100546
NEXT_PUBLIC_FIREBASE_APP_ID=1:777430100546:web:22125820d427b460301bdb

# Instrucciones de configuración:
# 1. Crea un proyecto en Firebase Console (https://console.firebase.google.com/)
# 2. Habilita Firestore Database
# 3. Habilita Authentication (Email/Password)
# 4. Habilita Firebase Storage
# 5. Copia este archivo como .env.local y completa los valores
# 6. Ve a Project Settings > General > Your apps y registra una web app
# 7. Copia la configuración y pégala aquí

# Configuración de Firestore Rules:
# Reglas sugeridas para Firestore:
# rules_version = '2';
# service cloud.firestore {
#   match /databases/{database}/documents {
#     // Allow read access to published posts
#     match /posts/{document} {
#       allow read: if resource.data.status == 'published';
#       allow write: if request.auth != null;
#     }
#   }
# }

# Configuración de Storage Rules:
# Reglas sugeridas para Storage:
# rules_version = '2';
# service firebase.storage {
#   match /b/{bucket}/o {
#     match /{allPaths=**} {
#       allow read: if true;
#       allow write: if request.auth != null;
#     }
#   }
# }

# Para crear un usuario administrador:
# 1. Ve a Firebase Console > Authentication
# 2. Crea un usuario con email y contraseña
# 3. Este usuario podrá acceder al panel de administración